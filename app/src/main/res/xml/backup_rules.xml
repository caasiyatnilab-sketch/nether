<?xml version="1.0" encoding="utf-8"?>
<full-backup-content>
    <!-- Exclude the encrypted local database from backups to avoid data leaks. -->
    <exclude domain="database" path="SecureAI.db"/>
</full-backup-content>
